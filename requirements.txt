# core packages — must stay on 0.3.x (1.x is a full rewrite, breaks create_react_agent)
langchain==0.3.25
langchain-core==0.3.64
langgraph==0.4.8

# OpenAI integration
langchain-openai==0.2.14
openai>=1.0.0

# tooling
pytest~=8.0
python-dotenv~=1.0
streamlit==1.45.1

spacy
inflect